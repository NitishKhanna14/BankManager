# url-shortner

## Setup

```python

0. <install python3 ,pip3>
1. git clone <this repo url>
2. cd url-shortner

$ source env/bin/activate (*linux only , chekout internet for other platforms)
(environment) $ pip3 install -r requirements.txt
     ...
     ...
     ...
(environment) $ pip3 flask run

```